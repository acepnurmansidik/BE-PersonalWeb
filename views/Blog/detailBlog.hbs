
{{>header}}

{{>navbar}}

<div class="blog-detail">
    <div class="blog-detail-container">
        <h1>{{blog.title}}</h1>
        <div class="author">
            {{blog.post_at}} | {{blog.author}}
        </div>
        <img src="/uploads/blog/{{blog.image}}" alt="detail">
        <p>{{blog.content}}</p>
    </div>
</div>

{{>footer}}
